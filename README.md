# xai_spy

# 🕵️ Prompt احترافي لبرمجة بوت لعبة الجاسوس على منصة WOLF Live

-----

## 📋 السياق والمتطلبات الأساسية

أنت مطور Node.js خبير متخصص في بناء بوتات لمنصة WOLF Live باستخدام مكتبة `wolf.js` (الإصدار 2.6.2) من مستودع `https://github.com/dawalters1/wolf.js.git`.

**المطلوب:** برمجة بوت لعبة الجاسوس (Spy Game Bot) في **ملف JavaScript واحد فقط** (`bot.js`) يعمل على منصة WOLF Live.

-----

## 🔧 إعداد المشروع

### بيانات تسجيل الدخول:

- **البريد الإلكتروني:** `scodoublet@yahoo.com`
- **كلمة المرور:** `12345`

### تثبيت الحزم المطلوبة:

```bash
npm install wolf.js
```

### هيكل الملف المطلوب:

- ملف واحد فقط: `bot.js`
- استخدام `import` أو `require` حسب ما تتطلبه المكتبة
- يجب أن يعمل بتشغيل: `node bot.js`

-----

## 🎮 وصف اللعبة الكاملة: “لعبة الجاسوس”

### المفهوم الأساسي:

لعبة اجتماعية تفاعلية يُرسل فيها البوت كلمة سرية (اسم فاكهة مختارة عشوائياً) لجميع اللاعبين **عبر الرسائل الخاصة** باستثناء لاعب واحد يُختار عشوائياً ليكون **الجاسوس**. يحاول اللاعبون كشف الجاسوس بينما يحاول الجاسوس إخفاء هويته.

-----

## 📝 المتطلبات التفصيلية

### 1️⃣ إنشاء اللعبة

**أوامر الإنشاء (في محادثة المجموعة/الغرفة):**

- `!جاسوس جديد`
- `!جس جديد`
- `!spy new` ← هذا الأمر يُعيِّن لغة اللعبة **إنجليزية**

**رسالة البوت بعد الإنشاء (عربي):**

```
/me يلا يا حلوين بدينا اللعبه انظموا للعبه بالأمر هذا "!جاسوس انظم او !جس انظم"
```

**رسالة البوت بعد الإنشاء (إنجليزي - عند استخدام `!spy new`):**

```
/me Come on, sweeties, we've started the game. Join the game with this command: "!spy join"
```

**قواعد الإنشاء:**

- لا يمكن إنشاء لعبة جديدة إذا كانت هناك لعبة نشطة بالفعل
- يُسجَّل مُنشئ اللعبة (sourceId) لاستخدامه لاحقاً في التحقق من الصلاحيات
- تُحدَّد لغة اللعبة (عربي/إنجليزي) عند الإنشاء وتُستخدم في جميع رسائل البوت طوال الجلسة

-----

### 2️⃣ الانضمام للعبة

**أوامر الانضمام:**

- `!جاسوس انظم`
- `!جس انظم`
- `!spy join` (للغة الإنجليزية)

**القواعد:**

- يمكن الانضمام فقط بعد إنشاء اللعبة وقبل بدئها
- لا يمكن للاعب الانضمام مرتين
- يُضاف اللاعب إلى قائمة اللاعبين بـ: `{ id, nickname, points: 0 }`
- الحد الأدنى للانضمام: **2 لاعبين** لبدء اللعبة

-----

### 3️⃣ بدء اللعبة

**أوامر البدء (من مُنشئ اللعبة فقط):**

- `!جاسوس بدء`
- `!جس بدء`
- `!spy start` (للغة الإنجليزية)

**عند بدء اللعبة يقوم البوت بما يلي بالترتيب:**

#### أ) اختيار الفاكهة السرية عشوائياً من القائمة المرفقة

#### ب) اختيار الجاسوس عشوائياً من بين اللاعبين

#### ج) إرسال كلمة السر للاعبين عبر رسائل خاصة (ما عدا الجاسوس):

```
كلمة السر هي: [اسم الفاكهة] 🍎
```

(إنجليزي):

```
The secret word is: [fruit name] 🍎
```

#### د) إرسال رسالة خاصة للجاسوس:

```
إنت الجاسوس يا قلب قلبي، لفّها عليهم واختار أي لاعب من القايمة تحسّه بيغلط في كشفك. كذا ما أحد بيشك فيك، وتقدر تراهن بعد إنه بيغلط في اختيارك 🥴
```

(إنجليزي):

```
You're the spy. Twist the game around and choose whoever you think won't figure you out. That keeps suspicion off you, and you can even bet they'll choose wrong 🥴
```

#### هـ) إرسال قائمة اللاعبين في المحادثة العامة:

```
🕵️ قائمة اللاعبين:
1- [ID] | [اسم اللاعب]
2- [ID] | [اسم اللاعب]
3- [ID] | [اسم اللاعب]
...
```

(إنجليزي):

```
🕵️ Players List:
1- [ID] | [nickname]
2- [ID] | [nickname]
...
```

-----

### 4️⃣ جولة التصويت والمراهنة

#### التصويت:

بعد عرض قائمة اللاعبين، يقوم كل لاعب مُنضم في اللعبة بإرسال **رقم اللاعب** الذي يعتقد أنه الجاسوس مباشرةً في المحادثة العامة.

- يُقبل الرقم فقط من اللاعبين المسجلين في اللعبة
- يصوت الجاسوس أيضاً لأي رقم للتمويه، ويقدر يراهن على أي شخص يقوم باختياره للتمويه أنه سيخطئ في اختياره
- لا تنتهي الجولة إلا بعد تصويت **جميع** اللاعبين

#### المراهنة (اختياري):

يتم التصويت بكتابة رقم اللاعب مباشرةً بعد ظهور قائمة اللاعبين، وبعد أن يتم اختيار اللاعب تأتي هذه الرسالة:

```
/me ارسل كمية النقاط الي بتراهن فيها
```

(إنجليزي):

```
/me Send the amount of points you are betting
```

**قواعد المراهنة:**

- الجاسوس يمكنه المراهنة على أي رقم للتمويه
- قواعد المراهنة على **الجميع** سواء جاسوس أم لاعب عادي
- من يصيب: يكسب **ضعف** ما راهن به محسومة من نقاط الخصم حتى لو كان رصيده صفراً أو تحت الصفر
- من يخطئ: يخسر **ضعف** ما راهن به محسومة من نقاطه لصالح الخصم حتى لو كان رصيد الخصم صفراً أو تحت الصفر
- لا يمكن المراهنة بأكثر من رصيد النقاط الحالي

**مثال توضيحي للمراهنة المتقاطعة:**

|الحالة                                                    |النتيجة                                                         |
|----------------------------------------------------------|----------------------------------------------------------------|
|اللاعب (أ) يراهن بـ 200 نقطة، اللاعب (ب) يراهن بـ 500 نقطة|—                                                               |
|إذا خسر اللاعب (أ)                                        |تذهب 200 نقطة من (أ) إلى (ب) + 300 نقطة إضافية من رصيد (أ)      |
|إذا خسر اللاعب (ب)                                        |يُحسم 200 نقطة فقط من (ب) لصالح (أ)، وتعود الـ 300 الباقية لـ (ب)|

-----

### 5️⃣ كشف الجاسوس

بعد تصويت **جميع** اللاعبين، يُرسل البوت في المحادثة العامة:

**عربي:**

```
/alert هذا هو الخاين البواق:
[ID الجاسوس] | [اسم الجاسوس]
```

**إنجليزي:**

```
/alert This is the traitor:
[Spy's ID] | [Spy's nickname]
```

-----

### 6️⃣ حساب النقاط

**السيناريوهات:**

|الموقف                        |النتيجة                             |
|------------------------------|------------------------------------|
|لاعب صوّت بشكل صحيح على الجاسوس|+1 نقطة للمصوِّت                      |
|لاعب صوّت بشكل خاطئ            |0 تغيير                             |
|الجاسوس: كل لاعب تمكن من كشفه |-1 نقطة من الجاسوس لكل لاعب كشفه    |
|نقاط الجاسوس تصل للسالب       |تُسجَّل بالسالب (مسموح بالنقاط السالبة)|
|مراهنة صحيحة                  |+ ضعف المبلغ المراهن                |
|مراهنة خاطئة                  |- ضعف المبلغ المراهن                |

-----

### 7️⃣ الاستمرار أو إنهاء اللعبة

بعد كشف الجاسوس، يُرسل البوت في المحادثة العامة:

**عربي:**

```
/me اذا ودك تكمل اللعبه ارسل رقم 1 او اذا مالك خاطر ارسل رقم 2
```

**إنجليزي:**

```
/me If you want to continue the game, send the number 1, or if you don't want to, send the number 2.
```

- يُقبل الاختيار **من مُنشئ اللعبة فقط**
- `1` → تبدأ جولة جديدة (يتم اختيار جاسوس جديد وفاكهة جديدة)
- `2` → تُغلق اللعبة نهائياً

-----

### 8️⃣ الإغلاق التلقائي (Timeouts)

|الحالة                            |المدة  |الإجراء                       |
|----------------------------------|-------|------------------------------|
|لم ينضم أحد بعد الإنشاء           |5 دقائق|إغلاق تلقائي                  |
|انضم لاعبون لكن لم تبدأ اللعبة    |5 دقائق|إغلاق تلقائي                  |
|بدأت اللعبة لكن لم يصوّت أحد       |5 دقائق|إغلاق تلقائي                  |
|بعض اللاعبين لم يصوتوا بعد 4 دقائق|4 دقائق|طرد غير المصوتين وإكمال اللعبة|
|مضى أسبوع كامل دون نشاط           |أسبوع  |يغادر البوت القناة تلقائياً    |

-----

### 9️⃣ طرد اللاعبين

**أوامر الطرد (من مُنشئ اللعبة فقط):**

- `!جاسوس طرد [رقم اللاعب]`
- `!جس طرد [رقم اللاعب]`
- `!spy kick [id]` (للغة الإنجليزية)

**مثال:** `!جاسوس طرد 3` ← يطرد اللاعب رقم 3 من قائمة اللعبة

-----

### 🔟 نظام الترتيب والإحصائيات

#### أ) الترتيب على مستوى الغرفة (أعلى 10):

**أوامر:**

- `!جاسوس ترتيب`
- `!جس ترتيب`
- `!spy rank`

**الشكل المطلوب:**

```
🏆 أفضل 10 لاعبين في الغرفة:
1-  [ID] | [الاسم المستعار] | 150 نقطة
2-  [ID] | [الاسم المستعار] | 120 نقطة
...
10- [ID] | [الاسم المستعار] | 30 نقطة
```

#### ب) الترتيب على مستوى التطبيق:

**أوامر:**

- `!جاسوس عام`
- `!جس عام`
- `!spy general`
- `!spy gl`

**الرسالة:**

```
ترتيبك العام: [رقم الترتيب] 🌍
```

#### ج) عرض مجموع النقاط:

**أوامر:**

- `!جاسوس مجموع`
- `!جس مجموع`
- `!spy total`

**الرسالة:**

```
مجموع نقاطك: [الرصيد] نقطة ✨
```

(يمكن أن يكون سالباً)

-----

### 1️⃣0️⃣ + 1 أوامر الوضع التلقائي وإنهاء اللعبة

#### الوضع التلقائي:

**أوامر:**

- `!جاسوس تلقائي`
- `!جس تلقائي`
- `!spy auto`

**الوصف:** يجعل البوت يعمل بشكل تلقائي بدون أن تأتي رسالة نهاية اللعبة (السؤال عن الاستمرار)، وتبدأ جولة جديدة تلقائياً بعد كل جولة.

#### إيقاف الوضع التلقائي:

**أوامر:**

- `!جاسوس توقف`
- `!جس توقف`
- `!spy stop`

**الوصف:** يوقف الوضع التلقائي ويعود للنمط العادي الذي يسأل عن الاستمرار في نهاية كل جولة.

#### إنهاء اللعبة:

**أوامر:**

- `!جاسوس انهاء`
- `!جس انهاء`
- `!spy end`

**الوصف:** ينهي اللعبة الجارية فوراً ويعيد ضبط الحالة بالكامل.

-----

### 1️⃣1️⃣ قائمة المساعدة

#### عربي:

**أوامر:**

- `!جاسوس مساعده`
- `!جاسوس مساعدة`
- `!جس مساعده`
- `!جس مساعدة`

**الرسالة:**

```
/me 🕵️ قائمة أوامر لعبة الجاسوس:
/ "!جاسوس جديد" أو "!جس جديد"                  ← لبدء لعبة جديدة
/ "!جاسوس انظم" أو "!جس انظم"                  ← للانضمام للعبة
/ "!جاسوس بدء" أو "!جس بدء"                    ← لبدء اللعبة
/ "!جاسوس طرد (رقم اللاعب)"                    ← لطرد لاعب
/ "!جاسوس ترتيب" أو "!جس ترتيب"                ← لعرض ترتيب اللاعبين في القناة
/ "!جاسوس عام" أو "!جس عام"                    ← لعرض الترتيب على مستوى التطبيق
/ "!جاسوس مجموع" أو "!جس مجموع"                ← لعرض مجموع نقاطك
/ "!جاسوس تلقائي" أو "!جس تلقائي"              ← للعب بشكل تلقائي
/ "!جاسوس توقف" أو "!جس توقف"                  ← لإيقاف اللعب التلقائي
/ "!جاسوس انهاء" أو "!جس انهاء"                ← لإنهاء اللعبة
/ "!جاسوس مساعده" أو "!جس مساعده"              ← لعرض هذه القائمة
```

#### إنجليزي:

**أوامر:**

- `!spy help`

**الرسالة:**

```
/me 🕵️ Spy Game Commands List:
/ "!spy new"                       ← to start a new game
/ "!spy join"                      ← to join the game
/ "!spy start"                     ← to start the game
/ "!spy kick (player number)"      ← to kick a player from the game
/ "!spy rank"                      ← to show player ranking in the channel
/ "!spy general" or "!spy gl"      ← to show your global ranking
/ "!spy total"                     ← to show your total points
/ "!spy auto"                      ← to play automatically
/ "!spy stop"                      ← to stop playing automatically
/ "!spy end"                       ← to end the game
/ "!spy help"                      ← to show this help menu
```

-----

## 🍎 قائمة الفواكه السرية (الكلمات السرية)

```javascript
const fruits = [
  'تفاح', 'برتقال', 'موز', 'فراولة', 'عنب', 'كرز', 'بطيخ', 'مانجو', 'أناناس', 'خوخ',
  'تين', 'رمان', 'جوافة', 'كمثرى', 'ليمون', 'يوسفي', 'مشمش', 'شمام', 'توت بري', 'جوز الهند',
  'أفوكادو', 'برقوق', 'فاكهة العاطفة', 'توت', 'لايمون', 'توت العليق الأسود', 'توت أزرق',
  'جوز دراق', 'بابايا', 'كيوي', 'ليمون أخضر', 'تمر', 'إجاص', 'نارنج', 'جريب فروت',
  'توت العليق', 'توت أسود', 'دوريان', 'فاكهة التنين', 'جاك فروت', 'رامبوتان', 'ليتشي',
  'كارامبولا', 'سالاك', 'كاكاو', 'سفارجل', 'بندق', 'لوز', 'كستناء', 'صنوبر',
  'أكاي', 'أسيرولا', 'أكي', 'مانجو أفريقي', 'أكيبي', 'فراولة جبال الألب', 'أماناتسو',
  'أمارا', 'أمباريلا', 'تفاح أمبروزيا', 'شمام أمبروزيا', 'أملا', 'أناناتو', 'أنونا',
  'تفاحة أمريكية', 'مايابل', 'أرونيا', 'باذنجان أفريقي', 'باشن فروت', 'بيل بيري',
  'بيغني', 'بيلبيري', 'بيلمبي', 'بلاك أبل', 'بلاك تشيري', 'بلاك كورانت', 'بلاك مولبيري',
  'بلاك راسبيري', 'بلاك سابوت', 'بلاكبيري', 'بلود أورانج', 'بلو باسيون فروت', 'بلو بيري',
  'بريد فروت', 'بروش تشيري', 'بوذا هاند', 'بورديكين بلوم', 'بوشيل أند بيري', 'جيلي بين',
  'باتر فروت', 'كاكتوس بير', 'كالاباش', 'كالامانسي', 'كامو كامو', 'كانيستيل', 'كانتالوب',
  'كيب غوسبيري', 'كارا كارا', 'كرامبولا', 'كاريسا', 'كاسكارا', 'كاشو أبل', 'كاتمون',
  'كافيار لايم', 'سيدار باي تشيري', 'سيمليديك', 'تشيمبيداك', 'سييلون غوسبيري',
  'تشاريتتشويلو', 'تشايوتي', 'تشيريمويا', 'تشيري بلوم', 'تشيكو فروت', 'تشوكولات فروت',
  'تشوكبيري', 'تشوكتشيري', 'سيترون', 'كليمنتين', 'كلودبيري', 'كلستر فيغ', 'كوكي أبل',
  'كوكو دي مير', 'كوكو بلوم', 'كوكونات', 'كوفي تشيري', 'كورنيليان تشيري', 'كراب أبل',
  'كرانبيري', 'كروبيري', 'كومكوات', 'كوبواكو', 'كورانت', 'كاستارد أبل', 'داباي',
  'دامسون', 'دامسون بلوم', 'دانغل بيري', 'دارلينغ بلوم', 'ديت', 'ديت بلوم',
  'دافيدسونز بلوم', 'ديد مانز فينغرز', 'ديكايزنيا', 'دوبل كوكونات', 'دراكونتو ميلون',
  'دراغون فروت', 'دوكو'
];
```

-----

## 🗃️ هيكل البيانات المقترح

```javascript
// حالة اللعبة
const gameState = {
  active      : false,
  autoMode    : false,   // وضع اللعب التلقائي
  language    : 'ar',    // 'ar' أو 'en'
  creatorId   : null,
  groupId     : null,
  players     : [],      // [{ id, nickname, points, hasVoted, vote, hasBet, betTarget, betAmount }]
  spyId       : null,
  secretFruit : null,
  phase       : 'idle',  // 'idle' | 'joining' | 'voting' | 'result'
  timers      : []
};

// قاعدة بيانات النقاط (دائمة عبر الجلسات - استخدم JSON file)
// { userId: { nickname, totalPoints } }
```

-----

## ⚙️ متطلبات التنفيذ التقني

### 1. هيكل الكود في ملف واحد `bot.js`:

```
bot.js
├── imports ومتغيرات الإعداد
├── قائمة الفواكه (fruits array)
├── إدارة حالة اللعبة (gameState object)
├── نظام النقاط (points management + JSON persistence)
├── دوال مساعدة (helper functions)
│   ├── getRandomFruit()
│   ├── getRandomSpy(players)
│   ├── sendPrivateMessage(client, userId, message)
│   ├── sendGroupMessage(client, groupId, message)
│   ├── buildPlayersList(players, language)
│   ├── resetGame()
│   └── clearTimers()
├── معالجات الأوامر (command handlers)
│   ├── handleNewGame(command, language)
│   ├── handleJoin(command)
│   ├── handleStart(command)
│   ├── handleVoteAndBet(command)
│   ├── handleKick(command)
│   ├── handleRank(command)
│   ├── handleGeneral(command)
│   ├── handleTotal(command)
│   ├── handleHelp(command, language)
│   ├── handleContinue(command)
│   ├── handleAutoMode(command)
│   └── handleEnd(command)
├── إعداد معالجات الأحداث (event listeners)
└── تسجيل الدخول وتشغيل البوت
```

### 2. متطلبات wolf.js API:

- استخدم `client.on('message', handler)` لمعالجة الرسائل
- استخدم `client.messaging.sendMessage(targetType, targetId, message)` لإرسال الرسائل
- `targetType`: `'group'` للمجموعات، `'private'` للرسائل الخاصة
- استخدم `message.sourceId` لمعرفة هوية المرسل
- استخدم `message.targetId` لمعرفة الغرفة/المجموعة
- استخدم `message.content` أو `message.body` لمحتوى الرسالة
- تسجيل الدخول: `client.login(email, password)`

### 3. استمرارية البيانات:

- احفظ نقاط اللاعبين في ملف `points.json` بجانب `bot.js`
- استخدم `fs.readFileSync` و `fs.writeFileSync` لقراءة/كتابة البيانات
- احمِ من أخطاء JSON باستخدام try/catch
- إذا لم يوجد الملف أنشئ `{}` فارغاً تلقائياً

### 4. معالجة الأخطاء:

- تعامل مع انقطاع الاتصال وإعادة الاتصال تلقائياً
- أضف `console.log` لتسجيل الأحداث المهمة
- تعامل مع الحالات الحافية (لاعب واحد، رسائل غير متوقعة)

-----

## 🧪 اختبار البوت

بعد كتابة الكود، قم بـ:

```bash
# 1. تثبيت الحزم
npm install wolf.js

# 2. تشغيل البوت
node bot.js
```

**تحقق من:**

- [ ] تسجيل الدخول بنجاح
- [ ] استقبال الرسائل من الغرفة
- [ ] إرسال الرسائل العامة والخاصة
- [ ] عمل مؤقتات الإغلاق التلقائي (5 دقائق و4 دقائق)
- [ ] مغادرة البوت القناة بعد أسبوع من عدم النشاط
- [ ] حفظ النقاط في `points.json`
- [ ] عمل نظام المراهنة وحساب النقاط بشكل صحيح
- [ ] عمل الوضع التلقائي `!spy auto`
- [ ] عمل أمر الإنهاء `!spy end`

-----

## ⚠️ تعليمات مهمة للمطور

1. **الملف الواحد:** كل الكود يجب أن يكون في `bot.js` فقط - لا ملفات إضافية عدا `points.json`
1. **التحقق من الصلاحيات:** فقط مُنشئ اللعبة يمكنه: البدء، الطرد، الاستمرار/الإيقاف، تفعيل/إيقاف الوضع التلقائي، إنهاء اللعبة
1. **العشوائية:** استخدم `Math.random()` لاختيار الفاكهة والجاسوس في كل جولة
1. **اللغة:** يتم تحديد لغة اللعبة مرة واحدة عند الإنشاء وتستمر حتى انتهاء اللعبة
1. **المؤقتات:** استخدم `setTimeout` و `clearTimeout` وتأكد من مسح المؤقتات عند إعادة تعيين اللعبة
1. **الرسائل الخاصة:** يُرسل البوت كلمة السر ورسالة الجاسوس عبر **رسائل خاصة** فقط
1. **النقاط السالبة:** مسموح لجميع اللاعبين (جاسوس وغير جاسوس) بامتلاك نقاط سالبة
1. **عدم التكرار:** اختر فاكهة مختلفة في كل جولة (يمكن إعادة الاختيار بعد استنفاد القائمة)
1. **الوضع التلقائي:** عند تفعيل `!spy auto` تبدأ جولة جديدة تلقائياً دون سؤال المنشئ
1. **مغادرة القناة:** إذا مضى أسبوع كامل دون نشاط يغادر البوت القناة تلقائياً

-----

## 📌 ملاحظة للمطور الذكاء الاصطناعي

**اكتب الكود الكامل للملف `bot.js` بناءً على هذا الـ prompt. اجعل الكود:**

- منظماً وقابلاً للقراءة مع تعليقات عربية/إنجليزية
- متوافقاً مع آخر إصدار من wolf.js (2.6.2)
- يعمل مباشرةً بعد `npm install wolf.js && node bot.js`
- يتعامل مع جميع الحالات الحافية المذكورة أعلاه
